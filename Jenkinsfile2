pipeline {
    agent any
    stages{
        stage("Git Checkout"){
            steps{
                git credentialsId: 'githubcreds', url: 'https://github.com/ansible777/java-app'
            }
        }
    }
}
