pipeline {
    agent any
    environment{
        PATH = "/usr/bin/git:$PATH"
    }    
    stages{
        stage("Git Checkout"){
            steps{
                git credentialsId: 'git', url: 'https://github.com/ansible777/java-app.git'
            }
        }
    }
}
